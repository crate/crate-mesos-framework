#!/bin/bash
./gradlew clean && ./gradlew snapshotRelease && vagrant ssh -c "rm -rf /tmp/crate-* && cp /vagrant/build/libs/crate-mesos-*-SNAPSHOT-*.jar /tmp/crate-mesos.jar"
curl -s -XPOST http://localhost:8080/v2/apps -d@marathon/crate-marathon.json -H "Content-Type: application/json" | jq .

